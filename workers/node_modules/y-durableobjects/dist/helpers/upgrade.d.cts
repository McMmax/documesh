/// <reference types="@cloudflare/workers-types" />
import * as hono from 'hono';
import { Env } from 'hono';

type Input = {
    outputFormat: "ws";
};
declare const upgrade: <E extends Env, P extends string>() => hono.MiddlewareHandler<E, P, Input>;

export { upgrade };
